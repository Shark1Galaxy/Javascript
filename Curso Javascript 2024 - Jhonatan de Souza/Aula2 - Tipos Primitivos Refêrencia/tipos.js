let nomeMeu = "Marcos"; // String literal, tanto faz '' ou "" ou `` (template literals);
let idadeMeu = 30; // Number Literal
let alturaMeu = 1.75; // Number
let estaAprovado = true || false; // Boolean
let sobrenomeMeu = undefined; // Undefined , difernte do Null , o undefined é quando a variavel foi declarada mas não inicializada
let corSelecionado = null; // Null pra dizer que litrealmente não tem valor. Caso queria redifinitirar a variavel depois por exepmlo: uma pagina, onde o usuario pode selecionar uma cor, mas no inicio não tem cor selecionada, então vc define como null ou seja Redfinifir o valor.

//Em javascript, diferente da linguagem C, numeros com ponto flutuante (float) e inteiros (int) são do mesmo tipo: Number, ou seja não existe int e float, como no C que vc precisa definir o tipo do numero

/*
String: A variável nomeMeu armazena texto entre aspas duplas. Strings representam sequências de caracteres e são usadas para dados textuais como nomes, endereços e mensagens.

Number: As variáveis idadeMeu e alturaMeu armazenam valores numéricos. Em JavaScript, não existe distinção entre inteiros e decimais—ambos são do tipo Number. Isso diferencia do JavaScript de linguagens como Java ou C, onde você precisaria especificar int ou float.

Boolean: A variável estaAprovado armazena um valor booleano (true ou false). Booleanos são fundamentais para lógica condicional e controle de fluxo em programas. Note a convenção de nomenclatura com prefixo esta para indicar que contém um valor verdadeiro/falso.

Undefined: A variável sobrenomeMeu é declarada mas não recebe nenhum valor atribuído. Quando você declara uma variável sem inicializá-la, JavaScript automaticamente atribui o valor undefined. Isso indica que a variável existe, mas ainda não tem um valor definido.

Null: A variável enderecoMeu é explicitamente atribuída a null. Diferentemente de undefined, null é uma atribuição intencional que representa "nenhum valor". É comum usar null quando você quer indicar deliberadamente que uma variável não possui um valor significativo.

Observação importante: Embora undefined e null pareçam similares, eles têm significados diferentes. Use null quando você quiser representar ausência de valor, e undefined ocorre naturalmente quando variáveis não são inicializadas.
 */

